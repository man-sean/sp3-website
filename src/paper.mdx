---
title: "SP³: Spherical Priors for Plug-and-Play Restoration"
authors:
  - name: Sean Man
    url: https://scholar.google.com/citations?user=VRCALiIAAAAJ&hl=en
    institution: Technion
  - name: Ron Raphaeli
    url: https://scholar.google.com/citations?user=kJeQaucAAAAJ&hl=en
    institution: Independent Researcher
    notes: ["*"]
  - name: Matan Kleiner
    url: https://scholar.google.com/citations?hl=en&user=n3R271gAAAAJ
    institution: Technion
    notes: ["*"]
  - name: Or Ronai
    url: https://scholar.google.com/citations?user=3tgII80AAAAJ
    institution: Technion
conference: Preprint
notes:
  - symbol: "*"
    text: equal contribution
links:
  - name: arXiv
    url: https://arxiv.org/abs/2606.16396
    icon: academicons:arxiv
  - name: Code
    url: https://github.com/man-sean/SP3
    icon: ri:github-fill

theme: light
favicon: sphere.svg
description: SP³ is a fast, gradient-free Plug-and-Play image restoration method that uses Spherical Encoders as generative priors.
thumbnail: thumbnail.jpg
---

import HighlightedSection from "./components/HighlightedSection.astro";
import Figure from "./components/Figure.astro";
import Picture from "./components/Picture.astro";
import TwoColumns from "./components/TwoColumns.astro";
import Wide from "./components/Wide.astro";
import ComparisonStrip from "./components/ComparisonStrip.astro";
import BibTeX from "./components/BibTeX.astro";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/starwind/tabs";

import hero from "./assets/sp3-hero.jpg";
import baselines from "./assets/sp3-baselines.png";
import afhqComparisons from "./assets/sp3-afhq-comparisons.jpg";
import celebaComparisons from "./assets/sp3-celeba-comparisons.jpg";
import anytime from "./assets/sp3-anytime.png";
import time from "./assets/sp3-time.png";
import projection from "./assets/sp3-projection.png";

<HighlightedSection>
  <p className="text-center text-xl">
    <strong>tl;dr:</strong> SP³ turns a Spherical Encoder into a fast generative
    prior for image restoration. It alternates stable manifold projections with
    closed-form data consistency, producing sharp results from the first
    iteration without computing gradients during inference.
  </p>
</HighlightedSection>

<Figure>
  <div
    slot="figure"
    className="grid w-full items-center gap-8 lg:grid-cols-[1.45fr_1fr]"
  >
    <Picture
      src={hero}
      alt="SP3 projects a degraded cat image through a spherical latent manifold, refining details over successive iterations"
      priority
    />
    <Picture
      src="../assets/sp3-runtime-hero.pdf"
      alt="Runtime and perceptual-quality comparison for AFHQ deblurring"
      priority
    />
  </div>
  <Fragment slot="caption">
    <strong>SP³ overview.</strong> The method repeatedly projects an estimate
    through a Spherical Encoder prior and enforces agreement with the measured
    data. It achieves competitive perceptual quality while running substantially
    faster than existing comparable zero-shot methods.
  </Fragment>
</Figure>

<HighlightedSection>

## Abstract

In this paper, we introduce SP³, a novel Plug-and-Play algorithm that accelerates maximum a posteriori image restoration by replacing denoisers with Spherical Encoders (SE) as generative priors. SP³ approximates the intractable proximal prior step by utilizing the SE tightly structured latent space as a robust projection onto the natural image manifold. Alternating this projection with a closed-form data-consistency step, via Half-Quadratic Splitting, achieves stable convergence without requiring gradient computation during inference. This unique formulation unlocks "anytime" restoration capabilities, producing sharp, plausible images from the first iteration. Evaluations across a variety of image restoration tasks demonstrate that SP³ achieves perceptual quality comparable to state-of-the-art zero-shot diffusion and flow methods while being $3$-$630\times$ faster.

</HighlightedSection>

## Why spherical priors?

Modern diffusion and flow priors produce strong restorations, but they are
iterative and often require backpropagation through a large generative model at
inference time. Spherical Encoders provide a different structure: they map
images to a densely populated, bounded latent sphere and decode points on that
sphere back to plausible images.

The full encoder-decoder therefore acts as a useful projection operator:

1. It maps degraded, out-of-distribution inputs toward the natural-image manifold.
2. Injecting latent noise stabilizes repeated projections and improves perceptual quality.
3. Successive projections refine the image instead of catastrophically drifting, unlike the tested VAE baselines.

<Figure>
  <Picture
    slot="figure"
    src={projection}
    alt="LPIPS and KID measurements showing stable repeated Sphere Encoder projections compared with VAE projections"
  />
  <Fragment slot="caption">
    <strong>Spherical Encoder as a projection operator.</strong> Repeated SE
    projections rapidly improve perceptual quality and remain stable. Standard
    VAE encode-decode cycles instead drift away from the image manifold.
  </Fragment>
</Figure>

### Why use both encoder and decoder?
<TwoColumns>
  <div slot="left">
Naive latent MAP baselines optimize only through the decoder. This discards the
encoder, even though it was jointly trained to map images onto the structured
spherical manifold.

SP³ uses the complete Spherical Encoder as its prior operator. In AFHQ
deblurring, it improves PSNR, LPIPS, KID, and runtime over both spherical
gradient descent baselines.

| Method | PSNR ↑ | LPIPS ↓ | KID$\times10^3$ ↓ | Time |
| :--- | ---: | ---: | ---: | ---: |
| S-GD | 18.5 | 0.58 | 34.3 | 1.32 s |
| S-PGD | 23.1 | 0.26 | 5.49 | 1.17 s |
| **SP³** | **25.7** | **0.12** | **2.64** | **0.28 s** |

  </div>
  <Figure slot="right">
    <Picture
      slot="figure"
      src={baselines}
      alt="Clean and degraded cat images above restoration results from SP3, spherical projected gradient descent, and spherical gradient descent"
    />
    <Fragment slot="caption">
      Top: clean and degraded. Bottom: SP³, S-PGD, and S-GD.
      SP³ balances data consistency with high perceptual quality.
    </Fragment>
  </Figure>
</TwoColumns>

<HighlightedSection>

## Plug-and-Play restoration

SP³ applies Half-Quadratic Splitting to alternate between the learned image
prior and the known measurement model. Starting from an initialization derived
from the degraded observation $y$, each iteration performs four operations:

<TwoColumns>
  <div slot="left">

1. **Encode:** $v_k \leftarrow E(x_{k-1})$
2. **Noisy spherify:** $v_k \leftarrow S(v_k,\sigma)$
3. **Decode:** $x_{\mathrm{prior}} \leftarrow D(v_k)$
4. **Data consistency:** $x_k \leftarrow \operatorname{prox}_{\lambda}(x_{\mathrm{prior}})$

The data step has the closed-form solution

$$
x_k=(A^\top A+\lambda I)^{-1}
(A^\top y+\lambda x_{\mathrm{prior}}).
$$

No differentiation or backpropagation is required during restoration.

  </div>
  <Figure slot="right">
    <Picture
      slot="figure"
      src="../assets/sp3-convergence.pdf"
      alt="Log-scale residual between consecutive SP3 iterations across six image restoration tasks"
    />
    <Fragment slot="caption">
      <strong>Empirical convergence.</strong> Consecutive iterates rapidly
      stabilize across all six evaluated restoration tasks.
    </Fragment>
  </Figure>
</TwoColumns>

</HighlightedSection>

## Restoration results

We evaluate SP³ on AFHQ-Cat at $256\times256$ resolution and CelebA at
$128\times128$ resolution across six noisy inverse problems: box inpainting,
denoising, super-resolution, deblurring, paintbrush inpainting, and random
inpainting.

<Tabs defaultValue="afhq">
  <TabsList>
    <TabsTrigger value="afhq">AFHQ-Cat</TabsTrigger>
    <TabsTrigger value="celeba">CelebA</TabsTrigger>
  </TabsList>
  <TabsContent value="afhq">
    <ComparisonStrip
      src={afhqComparisons}
      alt="AFHQ-Cat restoration comparisons across three degradation types"
      labels={["Clean", "Degraded", "SP³", "DDRM", "DPS", "DiffPIR", "PnP-Flow", "OT-ODE"]}
      caption_header="Qualitative results."
      caption="Inpainting, denoising, and super-resolution."
    />
  </TabsContent>
  <TabsContent value="celeba">
    <ComparisonStrip
      src={celebaComparisons}
      alt="CelebA restoration comparisons across three degradation types"
      labels={["Clean", "Degraded", "SP³", "DDRM", "DPS", "DiffPIR", "PnP-Flow", "OT-ODE"]}
      caption_header="Qualitative results."
      caption="Deblurring, paintbrush inpainting, and random inpainting."
    />
  </TabsContent>
</Tabs>

{/* <Wide> */}
  <Figure>
    <Picture
      slot="figure"
      src="../assets/sp3-metrics.pdf"
      alt="Perception-distortion plots comparing SP3 with Plug-and-Play, diffusion, and flow restoration methods"
      className="w-full!"
    />
    <Fragment slot="caption">
      <strong>Perception-distortion evaluation.</strong> SP³, shown as a red
      sphere, achieves perception-distortion tradeoffs comparable to the best
      competing zero-shot methods across AFHQ-Cat and CelebA.
    </Fragment>
  </Figure>
{/* </Wide> */}

<HighlightedSection>

## Fast and anytime restoration

Because a single Spherical Encoder pass already projects the degraded input
toward the natural-image manifold, SP³ produces a useful reconstruction after
one step and can refine it for as long as the available time budget permits.
With 20 steps, SP³ is approximately $8\times$ faster than OT-ODE and
$60\times$ faster than DPS, its closest competitors in visual quality.

  <Figure>
    <Picture
      slot="figure"
      src={time}
      alt="Perceptual quality versus runtime for SP3 and competing restoration methods"
    />
    <Fragment slot="caption">
      <strong>Quality versus runtime.</strong> SP³ points correspond to 1, 3,
      5, 10, and 20 iterations.
    </Fragment>
  </Figure>
  <Figure>
    <Picture
      slot="figure"
      src={anytime}
      alt="Clean and degraded cat images followed by SP3 super-resolution outputs after one, three, and five steps"
    />
    <Fragment slot="caption">
      Top: clean and degraded. Bottom: SP³ after 1, 3, and 5 steps.
    </Fragment>
  </Figure>

</HighlightedSection>

## BibTeX citation

<BibTeX text={String.raw`@misc{man2026sp3,
  title = {SP3: Spherical Priors for Plug-and-Play Restoration},
  author = {Man, Sean and Raphaeli, Ron and Kleiner, Matan and Ronai, Or},
  year = {2026},
  note = {Preprint}
}`} />
